# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Authentication
GET     /                           controllers.Application.home()
POST    /authenticate               controllers.Application.authenticate()
POST    /user                       controllers.Application.createUser()
GET     /session/:id                controllers.Application.getSession(id)

GET     /problem                    controllers.ProblemController.getAll()
POST    /problem                    controllers.ProblemController.create()

GET     /feature                    controllers.FeatureController.getAll()
POST    /feature                    controllers.FeatureController.create()

GET     /team                       controllers.TeamController.getAll()
POST    /team                       controllers.TeamController.create()

GET     /js/routes.js               controllers.Application.javascriptRoutes()

# Map static resources from the /public folder to the /assets URL path
GET     /img/*file                  controllers.Assets.at(path="/public/img", file)
GET     /js/*file                   controllers.Assets.at(path="/public/js", file)
GET     /css/*file                  controllers.Assets.at(path="/public/css", file)
GET     /templates/*file            controllers.Assets.at(path="/public/templates", file)
